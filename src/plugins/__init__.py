"""Hedera Agent Kit plugins exposing DeFi Copilot capabilities to an LLM.

Importing anything here requires the optional `agent` extra:

    pip install -e ".[agent]"

The core portfolio package deliberately has no such dependency, so balance
reads work without the kit's ~150-package transitive tree.
"""
